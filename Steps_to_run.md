# Install dependencies (if not already installed)
pip install -r requirements.txt

# Start the web server
python web_app.py

# Scan a target in terminal (authorized only!)
python main.py --target https://example.com